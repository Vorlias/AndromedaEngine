{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Project using CMake",
            "command": "${workspaceFolder}/build.sh",
            "args": [
                "--shaderc",
                "-E",
            ]
        }
    ]
}